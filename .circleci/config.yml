version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
        name: Build Docker Image
        command: docker-compose up --build
      - run:
        name: Run tests
        command: npm run test:e2e